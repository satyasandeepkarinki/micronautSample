apiVersion: apps/v1
kind: Deployment
metadata:
  name: "learning"
spec:
  selector:
    matchLabels:
      app: "learning"
  template:
    metadata:
      labels:
        app: "learning"
    spec:
      containers:
        - name: "learning"
          image: "gcr.io/learning/jib-image"
          ports:
            - name: http
              containerPort: 8080
          readinessProbe:
            httpGet:
              path: /health/readiness
              port: 8080
            initialDelaySeconds: 5
            timeoutSeconds: 3
          livenessProbe:
            httpGet:
              path: /health/liveness
              port: 8080
            initialDelaySeconds: 5
            timeoutSeconds: 3
            failureThreshold: 10
---
apiVersion: v1
kind: Service
metadata:
  name: "learning"
spec:
  selector:
    app: "learning"
  type: LoadBalancer
  ports:
    - protocol: "TCP"
      port: 8080